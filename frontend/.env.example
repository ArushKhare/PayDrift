# URL of the deployed backend API. Vite inlines this at build time, so on
# Render it must be set as an env var on the frontend service BEFORE building.
# Leave unset for local dev — App.jsx falls back to http://localhost:8000.
VITE_API_URL=https://your-backend.onrender.com
